---
apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: test
spec:
  selector:
    app: nginx
  type: LoadBalancer  # ClusterIP se change kiya
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 8000   # browser se access ke liye
